cryptography>=40.0.0
intelhex
click
cbor2
setuptools
pyyaml
pytest

# RFC 8554 LMS / HSS hash-based signatures, used by imgtool to
# generate keys and sign images for the IMAGE_TLV_LMS code path.
# Pure-Python; no compiled deps.
pyhsslms

# Needed by Mbed TLS 4.x's CMake-driven build (invoked from
# sim/mcuboot-sys/build.rs when the `mbedtls-v4` Cargo feature is
# enabled) to generate psa_crypto_driver_wrappers* and
# tf_psa_crypto_config_check_*.h. Harmless if that feature is never
# used.
jinja2
jsonschema
